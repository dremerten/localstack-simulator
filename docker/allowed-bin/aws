#!/bin/sh
set -eu

export AWS_PAGER=""

case " $* " in
  *" --endpoint-url "*)
    exec /workspace/.aws-cli-bin/aws "$@"
    ;;
  *)
    exec /workspace/.aws-cli-bin/aws --endpoint-url "${LOCALSTACK_ENDPOINT:-http://localstack:4566}" "$@"
    ;;
esac
